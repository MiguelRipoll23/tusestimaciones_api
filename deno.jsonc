{
  "tasks": {
    "run": "deno run --allow-env --allow-net --watch mod.ts",
    "compile": "deno compile --allow-env --allow-net --output server mod.ts",
    "fmt": "deno fmt",
    "lint": "deno lint"
  }
}
