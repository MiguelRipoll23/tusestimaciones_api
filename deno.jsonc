{
  "tasks": {
    "run": "deno run --allow-env --allow-net --watch main.ts",
    "compile": "deno compile --allow-env --allow-net --output server main.ts",
    "fmt": "deno fmt",
    "lint": "deno lint"
  }
}
